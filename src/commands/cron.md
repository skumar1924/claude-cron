---
description: Open the cron job dashboard (starts server if needed)
allowed-tools: Bash(node *)
---

Run this command exactly:
`node ~/.claude/plugins/local/claude-cron/commands-js/cron.js`

Output exactly what the script prints and nothing else.
