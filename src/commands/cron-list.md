---
description: List all cron jobs
allowed-tools: Bash(node *)
---

Run this command exactly:
`node ~/.claude/plugins/local/claude-cron/commands-js/cron-list.js`

Print the output exactly as returned and nothing else.
